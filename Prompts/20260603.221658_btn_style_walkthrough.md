User Prompt:
"Join call" button, kep same look and feel as "Start call" button

# Walkthrough - Match Button Look and Feel

Updated the "Join Call" button design on the Home Fragment to align with the "Start Call" button layout.

## Changes Made

### Layout Styling

#### [fragment_home.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/res/layout/fragment_home.xml)
- Removed `style="@style/Widget.Material3.Button.ElevatedButton"` from `btn_join_call` so it defaults to the standard filled button styling, matching `btn_start_call`.
