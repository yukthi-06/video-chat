# User Prompt
once two participants are connected, there is no longer need for the signalling server.
Signallling server is only needed at the end of the call.
What if the server is shutdown after the connection is made..
How does one party disconnecting the call intimate the other party?
The app , in addition to sending end signal to server, must also send disconnect signal directly to the participants of the call.

---

# Tasks - Direct Peer Disconnection Detection

- `[x]` Modify `WebRtcClient.java` to declare `onPeerDisconnected()` callback and check `IceConnectionState`
- `[x]` Modify `CallActivity.java` to implement `onPeerDisconnected()` and call `endCall()`
