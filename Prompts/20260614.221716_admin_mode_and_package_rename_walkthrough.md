# User Prompt
On the left side menu, create a new menu item with label "Admin Mode".
On this screen, there should be a password field.
On entering the right password, that user becomes an 'Admin' user.
The right password is stored in the application properties file, stored somewhere in app/src/main/res/

# Walkthrough - Package Rename & Admin Mode Feature

I have successfully updated the package name across the codebase to `com.vypeensoft.videochatapp` and implemented the **Admin Mode** feature.

## Changes Made

### Package Refactoring (`com.example` -> `com.vypeensoft`)
- **Gradle Config**: Updated `namespace` and `applicationId` to `com.vypeensoft.videochatapp` in [build.gradle](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/build.gradle).
- **Manifest**: Updated root package to `com.vypeensoft.videochatapp` in [AndroidManifest.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/AndroidManifest.xml).
- **Java Sources**: Updated package declaration header and all internal R class/BuildConfig imports across all Java files to reference `com.vypeensoft.videochatapp`.

### Admin Mode Implementation
- **Properties File**: Created [app.properties](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/res/raw/app.properties) under `res/raw/` containing `admin.password=admin123`.
- **Menu Item**: Added "Admin Mode" with ID `nav_admin_mode` inside the left drawer menu [drawer_menu.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/res/menu/drawer_menu.xml).
- **Strings**: Configured UI labels, hint text, and status state strings in [strings.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/res/values/strings.xml).
- **Layout**: Designed a premium and clean user interface in [activity_admin_mode.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/res/layout/activity_admin_mode.xml) containing password entry fields, action buttons, and status indicator.
- **Controller/Logic**: Implemented [AdminModeActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/java/com/vypeensoft/videochatapp/activities/AdminModeActivity.java) to load the password from raw properties, check user inputs, update persistent admin status in `SharedPreferences`, and provide feedback toasts.
- **Integration**: Connected navigation menu item selection in [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/java/com/vypeensoft/videochatapp/activities/MainActivity.java) to launch the `AdminModeActivity`.

---

## Verification

Since local terminal access is restricted:
1. Please compile and build the APK using the gradle command:
   ```cmd
   gradlew.bat assembleDebug
   ```
2. Run the application on an emulator or device.
3. Open the navigation drawer from the top-left, click on **Admin Mode**, type `admin123`, and hit **Submit** to verify that your status upgrades to **Admin**.
