# User Prompt
there is lot of white space at the top of the screen.  raise all elements towards the upper part of the screen. (homescreen)

# Goal Description
The user wants to reduce the empty vertical space at the top of the home screen. We will modify `fragment_home.xml` to reduce the top margin of the welcome title, subtitle, and the call cards.

## Proposed Changes

### Layout
#### [MODIFY] [fragment_home.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/res/layout/fragment_home.xml)
- Reduce `layout_marginTop` of `tv_welcome` from `32dp` to `8dp`.
- Reduce `layout_marginTop` of `card_start_call` from `48dp` to `20dp`.
- Reduce `layout_marginTop` of `card_join_call` from `24dp` to `16dp`.

## Verification Plan
- Build the project and verify XML parses correctly.
