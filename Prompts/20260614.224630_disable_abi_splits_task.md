# User Prompt
keep APK name same as ok

# Disable ABI Splits Task List

- [x] Change `enable true` to `enable false` inside the `splits.abi` block in `app/build.gradle`
- [ ] Verify build compiles and produces standard `app-debug.apk` filename
