# User Prompt
set 'X:\Projects_X\0_Active\8_Android_APK\04_Early_Stage\Video_Chat_APK_GITHUB\app-icon.png' as app icon

# Walkthrough

We have prepared the necessary configurations and scripts to set `app-icon.png` as the Android launcher icon.

## Changes Made
1. **[AndroidManifest.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/AndroidManifest.xml)**:
   - Updated `android:icon` and `android:roundIcon` to point to `@mipmap/ic_launcher` instead of `@android:drawable/presence_video_online`.
2. **[resize_icon.ps1](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/resize_icon.ps1)**:
   - Created a PowerShell script to automatically resize `app-icon.png` to all required mipmap sizes:
     - `mipmap-mdpi` (48x48)
     - `mipmap-hdpi` (72x72)
     - `mipmap-xhdpi` (96x96)
     - `mipmap-xxhdpi` (144x144)
     - `mipmap-xxxhdpi` (192x192)

## Next Steps / Verification Required
Due to system-level permissions restricting terminal execution (Access is denied), you will need to run the generated PowerShell script to resize and output the icons:

```powershell
powershell -ExecutionPolicy Bypass -File .\resize_icon.ps1
```

Once executed, verify that:
- The folders under `app/src/main/res/` now contain `mipmap-*/ic_launcher.png`.
- Run your build to verify the app compilation succeeds.
