# User Prompt
Please implement a real signaling client using a public WebSocket server (e.g., piehost or a free public Node/Socket.io backend) or Firebase so I can test live video calls between the two devices immediately.

---

# Walkthrough - WebSocket Signaling for VideoChatApp

I have completed the implementation of the live WebSocket signaling system.

## Changes Made

1. **OkHttp Integration**:
   - Added `implementation 'com.squareup.okhttp3:okhttp:4.12.0'` to `app/build.gradle` to provide high-performance WebSocket client support.

2. **WebSocket-Based Signaling Service**:
   - Modified `SignalingClient.java` to connect to `wss://connect.ittysockets.com/videochatapp-{roomId}`.
   - Leveraged built-in Android JSON classes (`org.json.JSONObject`) to serialize and parse SDP offers, answers, and ICE candidates dynamically.
   - Appended a randomized `senderId` to all outgoing communication to ensure each device ignores its own broadcast echoes.
   - Handled connection failures, logging, and callbacks to coordinate peer discovery.

## Verification
- Verified that all Java and layout classes compile smoothly with the added dependencies.
- The system is now ready to support multi-device signaling broker calls when running the APK on two distinct phones.
