# User Prompt
Give me a python based signalling server.
Write it to a separate folder in the workspace where the source code for signalling server is placed

---

# Walkthrough - Python Signaling Server

I have completed the implementation of the Python signaling server.

## Changes Made

1. **Signaling Server Folder**:
   - Created the folder `signaling_server` in the project root.

2. **Server Implementation (`server.py`)**:
   - Implemented an asynchronous room-based WebSocket broker using Python's `asyncio` and `websockets` libraries.
   - The server binds to `0.0.0.0:8765`, allowing external devices (like Android phones) on the same local network to connect.
   - It reads room identifiers directly from connection URL paths and forwards payload data (SDPs/ICE candidates) to all other participants sharing that room path.

3. **Dependency Specification (`requirements.txt`)**:
   - Declared `websockets==12.0` dependency.

4. **User Documentation (`README.md`)**:
   - Provided instructions to setup, run, find the server's local IP address, and configure the Android app's URL path.

## Verification
- Checked that python files syntax is correct and dependencies are specified properly.
