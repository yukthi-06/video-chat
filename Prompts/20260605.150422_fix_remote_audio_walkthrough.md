# User Prompt
there is no audio in remote video. please fix

# Walkthrough

We have corrected the WebRTC configuration to support audio on remote connections.

## Changes Made
1. **[WebRtcClient.java](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/java/com/example/videochatapp/webrtc/WebRtcClient.java)**:
   - Updated the `onAddTrack` callback to listen for `AudioTrack` and call `setEnabled(true)` on it when the track is added dynamically.
   - Updated the answer media constraints in `handleOffer` / `createAnswer` to explicitly require receiving audio and video tracks (adding key/value pairs for `OfferToReceiveAudio` and `OfferToReceiveVideo`).

## Verification Results
- Please compile and test the app with a remote peer connection. You should now hear the remote user's audio output successfully.
