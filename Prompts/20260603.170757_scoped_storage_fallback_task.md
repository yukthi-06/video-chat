# User Prompt
recordings folder got created, but no MP4 files

---

# Tasks - Scoped Storage Writable Directory Fallback

- `[ ]` Add `getRecordingsDirectory()` helper in `CallActivity.java` to dynamically check and select the best write path
- `[ ]` Update path assignments in `initWebRtcAndSignaling()` to utilize `getRecordingsDirectory()`
