# User Prompt
Update the Signalling server to accomodate the new changes

# Update Signaling Server Plan

Improve the signaling server to handle multi-peer rooms correctly by broadcasting the `peer_joined` notification to all existing peers when any new user joins (instead of restricting it to only the second peer).

## Proposed Changes

### Signaling Server

#### [MODIFY] [server.py](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/signaling_server/server.py)
- Update registration logic to broadcast `peer_joined` to all other active peers in the room when a new client registers.

## Verification Plan

### Manual Verification
- Run the server script and verify logs output client join messages correctly.
