# User Prompt
once any user disconnects the call, wait till the last piece is uploaded and then actual disconnect takes place.

# Delayed Disconnect on Upload Completion Task List

- [x] Update `stop()` in `WebRtcVideoRecorder.java` to invoke `onSegmentCompleted()` for the final segment.
- [x] Add `activeUploadsCount` (AtomicInteger) and `isDisconnectPending` (boolean) to `CallActivity.java`.
- [x] Increment `activeUploadsCount` when `sendVideoFile()` starts, and decrement when it finishes/fails.
- [x] Update `endCall()` in `CallActivity.java` to perform a deferred disconnect check loop waiting for uploads to hit zero.
