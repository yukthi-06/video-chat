# User Prompt
the 60 second for cutting the video into chunks, please make this configurable in app.properties

# Walkthrough - Configurable Video Chunk Duration

I have successfully updated the application to read the video recording segment duration dynamically from the configuration properties file.

## Changes Made

### Configuration Settings (`app.properties`)
- **Added Property**: Configured `video.chunk.duration.seconds=60` inside the raw resource [app.properties](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/res/raw/app.properties).

### Dynamic Recorder Duration (`WebRtcVideoRecorder.java`)
- **Setter Method**: Added `segmentDurationNs` field and a corresponding `setSegmentDurationSeconds(int seconds)` configuration setter to [WebRtcVideoRecorder.java](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/java/com/vypeensoft/videochatapp/webrtc/WebRtcVideoRecorder.java).
- **Split Logic**: Replaced the hardcoded nanoseconds value (`60000000000L`) with the configurable `segmentDurationNs` variable in the frame processing check.

### Call Activity Controller (`CallActivity.java`)
- **Property Loader**: Implemented `loadVideoChunkDuration()` in [CallActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/java/com/vypeensoft/videochatapp/activities/CallActivity.java) to parse `video.chunk.duration.seconds` (defaulting to 60 if missing or invalid).
- **Binding**: Passed this value to the local and remote recorders upon initialization using `setSegmentDurationSeconds()`.

---

## Verification

Please run the build command to compile:
```cmd
gradlew.bat assembleDebug
```
For testing:
1. Change `video.chunk.duration.seconds` in `app.properties` to a small value (e.g., `10` for ten-second chunks).
2. Build and launch the application.
3. Start a call and check that your local video files are split and uploaded to the admin every 10 seconds.
