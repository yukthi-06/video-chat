# User Prompt
keep APK name same as ok

# Walkthrough - Disable ABI Splits

I have successfully updated the build configuration to disable ABI splits, which restores standard output APK filenames like `app-debug.apk`.

## Changes Made

### build.gradle Updates
- **Disabled Splits**: Set `enable false` inside the `splits.abi` block in [build.gradle](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/build.gradle). This bundles all specified architecture binaries into a single, unified APK instead of compiling target-specific files.

---

## Verification

To compile the APK with the standard filename:
1. Run the compilation command:
   ```cmd
   gradlew.bat assembleDebug
   ```
2. Verify that `app-debug.apk` is generated under `app/build/outputs/apk/debug/`.
