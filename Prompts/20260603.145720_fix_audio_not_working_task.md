# User Prompt
no sound during video chat

---

# Tasks - Fix Call Audio

- `[x]` Modify `WebRtcClient.java` to construct `JavaAudioDeviceModule` and pass it to factory builder
- `[x]` Modify `CallActivity.java` to set mode to `MODE_IN_COMMUNICATION` and enable speakerphone on call start, and reset it on destroy
