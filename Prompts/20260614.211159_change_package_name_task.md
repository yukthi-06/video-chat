# User Prompt
change package from 'com.example' to 'com.vypeensoft'

# Package Name Refactoring Task List

- [ ] Reorganize Java directories to use `com/vypeensoft` instead of `com/example`
- [ ] Update package declarations and import statements in all Java files
- [ ] Update `app/build.gradle` (namespace and applicationId)
- [ ] Update `app/src/main/AndroidManifest.xml` (package name)
- [ ] Run `gradlew.bat assembleDebug` to verify project builds successfully
