# User Prompt
keep APK name same as ok

# Disable ABI Splits Plan

Modify build configuration to disable ABI splits, reverting back to building a single universal APK (maintaining standard names like `app-debug.apk`).

## User Review Required

> [!IMPORTANT]
> ABI splits will be disabled, which returns the output APK filename to standard `app-debug.apk` and `app-release.apk`. However, the APK will be larger since it bundles both target architecture binaries together.

## Proposed Changes

### Build Configuration

#### [MODIFY] [build.gradle](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/build.gradle)
- Change `enable true` to `enable false` in the `splits.abi` configuration.

## Verification Plan

### Automated Tests
- Compile the debug build to ensure `app-debug.apk` is generated:
  ```cmd
  gradlew.bat assembleDebug
  ```
