# User Prompt
after changing to ws://, nothing appears in log when two mobiles try to connect

---

# Implementation Plan - Allow Cleartext Traffic

This plan outlines adding the cleartext traffic configuration in `AndroidManifest.xml` to allow Android OS to permit unencrypted WebSocket (`ws://`) connections to our local Python signaling server.

## Proposed Changes

### Manifest Configuration

#### [MODIFY] [AndroidManifest.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/app/src/main/AndroidManifest.xml)
- Add `android:usesCleartextTraffic="true"` to the `<application>` element.
