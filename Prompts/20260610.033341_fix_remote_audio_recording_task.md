# User Prompt
```
fix the recording of Remote Audio Track
```

# Tasks - Remote Audio Track Recording Support

- `[x]` Attempt to intercept remote audio track via `AudioTrack.addSink(AudioSink)`.
  - Result: Failed compilation because `org.webrtc.AudioSink` is not defined in the `im.conversations.webrtc:webrtc-android:119.0.0` library.
- `[x]` Revert changes in `WebRtcClient.java` and `CallActivity.java` to restore a working clean build.
- `[x]` Document the limitation in the walkthrough file.
