# User Prompt
```
fix the recording of Remote Audio Track
```

# Tasks - Remote Audio Track Recording Support

- `[x]` Update WebRtcClient to support remote audio sample capture.
  - `[x]` Check if `AudioTrack.addSink(AudioSink)` or `JavaAudioDeviceModule.Builder.setPlayoutSamplesReadyCallback` is available.
  - `[x]` Pipe remote audio samples to `WebRtcListener`.
- `[x]` Update CallActivity to route remote audio samples.
  - `[x]` Instantiate `remoteRecorder` with `recordAudio = true`.
  - `[x]` Feed remote audio samples to `remoteRecorder` in `onRemoteAudioSample(...)`.
- `[x]` Verify compilation and test.
