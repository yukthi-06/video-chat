# User Prompt
in the signalling server, add more logging statements.... 
IP address of peer who joined.  
Assign a identifier for the peer
what string messages are exchanged between peers
etc...

# Verbose Signaling Server Logging Plan

Add enhanced logging to the signaling server including peer IP addresses, unique connection identifiers, and structured logs of message exchanges (with base64 payload truncation to prevent flooding).

## Proposed Changes

### Signaling Server

#### [MODIFY] [server.py](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/signaling_server/server.py)
- Import `uuid` for unique peer identification.
- Store map of WebSockets to unique short IDs.
- Retrieve peer IP using `websocket.remote_address[0]`.
- Log connection events with Peer ID, IP address, and Room ID.
- Log message exchanges, parsing and truncating large payloads (like chunked video data) to keep the logs readable.

## Verification Plan

### Manual Verification
- Start the server:
  ```bash
  python signaling_server/server.py
  ```
- Establish connections and exchange messages to verify formatted logs in the console.
