# User Prompt
the 60 second for cutting the video into chunks, please make this configurable in app.properties

# Configurable Chunk Duration Task List

- [x] Add `video.chunk.duration.seconds=60` to `app/src/main/res/raw/app.properties`.
- [x] Add `segmentDurationNs` field and `setSegmentDurationSeconds` setter to `WebRtcVideoRecorder.java`.
- [x] Use `segmentDurationNs` instead of `60000000000L` in `WebRtcVideoRecorder.java`.
- [x] Load properties and set duration on recorders in `CallActivity.java`.
