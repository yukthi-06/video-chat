# Task list: Split Call Recording Every 60 Seconds

- [x] Parse `filePath` into `baseFilePath` and `fileExtension` in `WebRtcVideoRecorder` constructor
- [x] Construct the sequential output path dynamically in `startInternal()`
- [x] Implement `splitRecording()` to stop and reset encoders/muxer safely
- [x] Monitor PTS in `drawAndEncodeFrame()` and trigger `splitRecording()` at 60 seconds
- [x] Verify compiling/building (User opted to skip compilation check)
