# User Prompt
Update the Signalling server to accomodate the new changes

# Walkthrough - Update Signaling Server

I have successfully updated the signaling server code to broadcast client connection states correctly in multi-peer rooms.

## Changes Made

### Server code (`server.py`)
- **Broadcasting peer_joined**: Modified `register(websocket, room_id)` in [server.py](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Video_Chat_APK_GITHUB/signaling_server/server.py) to iterate and send `"type": "peer_joined"` to all other active connections in the room when a new peer connects, rather than only notifying when the room size equals two.
- **Message Passthrough**: Since the server already broadcasts any arbitrary incoming string message to all other participants via:
  ```python
  await asyncio.gather(*[ws.send(message) for ws in other_peers])
  ```
  It will automatically broadcast the new chunked video transmission packets (`admin_announcement`, `video_file_start`, `video_file_chunk`, and `video_file_end`) directly to destination peers without needing custom parsing in Python.

---

## Verification

To verify the signaling server updates:
1. Start the signaling server:
   ```bash
   python signaling_server/server.py
   ```
2. Connect multiple WebSockets or Android devices to the same room.
3. Check the server console log outputs to confirm that every new peer connection triggers notifications to all other active peers.
